#!/bin/bash

function push_commit()
{
    git commit -am bash
    git push
}



if [ "$1" == 'push' ]; then
    # call function
    push_commit
    exit 0

if [ "$1" == 'run' ]; then
    # push code
    push_commit
    # run Python script
    python3 rename_file.py
    exit 0

fi
